/**
 * <h1>Prerequisites</h1>
 * HAL's code generation relies on the code generation used in UberFire. If UberFire changes their code
 * generation setup (currently based on freemarker templates), HAL's code generation will break!
 */
package org.jboss.hal.processors;